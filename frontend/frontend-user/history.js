// Page-specific logic for history.html (M4 - user-facing simplified history view).
// Phase 1: shell only. Phase 2 (M4) wires this to GET /api/payments/{id}/history
// and renders it via lifecycle-timeline.js.

document.getElementById("load-history-btn").addEventListener("click", function () {
  throw new Error("Not implemented yet - Phase 2 (M4)");
});
