// Page-specific logic for index.html (M1 - new payment form).
// Phase 1: shell only. Phase 2 (M1) wires this to POST /api/payments.

document.getElementById("new-payment-form").addEventListener("submit", function (event) {
  event.preventDefault();
  throw new Error("Not implemented yet - Phase 2 (M1)");
});
